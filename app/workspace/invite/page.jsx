import UserSearchAndInvite from "./client";

export const metadata = {
  title: "User Search and Invite: Your App Name",
  description: "Search for users and invite them to your workspace.",
};

const Page = () => {
  return <UserSearchAndInvite />;
};

export default Page;
import mongoose from 'mongoose';
  
// for when a sponsor wants to give the product they have
// purchased to the tournament prize pool
const sponsorToTournamentPrizePoolOwnershipSchema = {
    // to be impletemented later
};

export default sponsorToTournamentPrizePoolOwnershipSchema;

import mongoose from 'mongoose';
  
// for when a product put as the tournment prize is won
const tournamentWinOwnershipSchema = {
    // to be impletemented later
};

export default tournamentWinOwnershipSchema;
